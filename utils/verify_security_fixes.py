#!/usr/bin/env python3
\"\"\"
Quick verification script for critical security fixes
Run this to confirm HTTPOnly cookies and CORS restrictions are working
\"\"\"

import requests
import json
from urllib.parse import urljoin

# Configuration
API_BASE_URL = "http://localhost:8000"
FRONTEND_URL = "http://localhost:3000"

def print_section(title):
    \"\"\"Print a section header\"\"\"
    print(f\"\\n{'='*60}\")
    print(f\"  {title}\")
    print('='*60)

def test_cors_headers():
    \"\"\"Test CORS header restrictions\"\"\"
    print_section(\"🔍 Test 1: CORS Headers Restriction\")
    
    headers = {
        "Origin": FRONTEND_URL,
        "Content-Type": "application/json"
    }
    
    try:
        response = requests.options(
            urljoin(API_BASE_URL, "/login"),
            headers=headers
        )
        
        print(\"\\n✓ Preflight Request Sent\")\n        print(f\"Status: {response.status_code}\")\n        \n        cors_headers = {\n            \"Allow-Origin\": response.headers.get(\"Access-Control-Allow-Origin\"),\n            \"Allow-Methods\": response.headers.get(\"Access-Control-Allow-Methods\"),\n            \"Allow-Headers\": response.headers.get(\"Access-Control-Allow-Headers\"),\n            \"Max-Age\": response.headers.get(\"Access-Control-Max-Age\")\n        }\n        \n        print(\"\\n✓ CORS Headers:\")\n        for key, value in cors_headers.items():\n            if value:\n                print(f\"  {key}: {value}\")\n        \n        # Verify restricted headers\n        allow_methods = cors_headers[\"Allow-Methods\"] or \"\"\n        if \"*\" in allow_methods:\n            print(\"\\n❌ FAILED: Wildcard methods still allowed!\")\n            return False\n        \n        if all(method in allow_methods for method in [\"GET\", \"POST\", \"PUT\", \"DELETE\"]):\n            print(\"\\n✓ PASSED: Only required HTTP methods allowed\")\n        else:\n            print(\"\\n❌ FAILED: Missing required methods\")\n            return False\n        \n        allow_headers = cors_headers[\"Allow-Headers\"] or \"\"\n        if \"*\" in allow_headers:\n            print(\"❌ FAILED: Wildcard headers still allowed!\")\n            return False\n        \n        if \"Authorization\" in allow_headers and \"Content-Type\" in allow_headers:\n            print(\"✓ PASSED: Only required headers allowed\")\n        else:\n            print(\"❌ FAILED: Missing required headers\")\n            return False\n        \n        return True\n        \n    except Exception as e:\n        print(f\"❌ ERROR: {str(e)}\")\n        return False\n\ndef test_httponly_cookies():\n    \"\"\"Test HTTPOnly cookie implementation\"\"\"
    print_section(\"🔍 Test 2: HTTPOnly Cookies\")\n    \n    # Mock login data\n    login_data = {\n        \"username\": \"admin\",\n        \"password\": \"test123\"  # This will likely fail, but we're checking headers\n    }\n    \n    try:\n        response = requests.post(\n            urljoin(API_BASE_URL, \"/login\"),\n            json=login_data,\n            headers={\"Origin\": FRONTEND_URL}\n        )\n        \n        print(f\"\\n✓ Login Request Status: {response.status_code}\")\n        \n        # Check Set-Cookie headers\n        cookies = response.headers.getlist(\"Set-Cookie\")\n        \n        if not cookies:\n            print(\"⚠️  No cookies set (login may have failed)\")\n            print(f\"Response: {response.text[:200]}\")\n            return None\n        \n        print(f\"\\n✓ Found {len(cookies)} cookie(s):\")\n        \n        httponly_ok = True\n        secure_ok = True\n        samesite_ok = True\n        \n        for cookie in cookies:\n            print(f\"\\n  Cookie: {cookie.split('=')[0]}\")\n            \n            if \"HttpOnly\" in cookie:\n                print(f\"    ✓ HttpOnly flag set\")\n            else:\n                print(f\"    ❌ HttpOnly flag MISSING\")\n                httponly_ok = False\n            \n            if \"Secure\" in cookie:\n                print(f\"    ✓ Secure flag set\")\n            else:\n                print(f\"    ⚠️  Secure flag not set (OK for localhost)\")\n            \n            if \"SameSite=Strict\" in cookie or \"SameSite=strict\" in cookie:\n                print(f\"    ✓ SameSite=Strict set\")\n            elif \"SameSite\" in cookie:\n                print(f\"    ⚠️  SameSite set but not Strict: {cookie}\")\n                samesite_ok = False\n            else:\n                print(f\"    ❌ SameSite MISSING\")\n                samesite_ok = False\n        \n        if httponly_ok and samesite_ok:\n            print(\"\\n✓ PASSED: Cookies properly configured\")\n            return True\n        else:\n            print(\"\\n❌ FAILED: Cookie configuration issues\")\n            return False\n        \n    except Exception as e:\n        print(f\"❌ ERROR: {str(e)}\")\n        return False\n\ndef test_cors_wildcard_rejection():\n    \"\"\"Test that wildcard origins are properly rejected\"\"\"
    print_section(\"🔍 Test 3: Wildcard Origin Rejection\")\n    \n    headers = {\n        \"Origin\": \"http://attacker.com\",\n        \"Content-Type\": \"application/json\"\n    }\n    \n    try:\n        response = requests.get(\n            urljoin(API_BASE_URL, \"/\"),\n            headers=headers\n        )\n        \n        cors_origin = response.headers.get(\"Access-Control-Allow-Origin\")\n        \n        if cors_origin is None:\n            print(\"\\n✓ PASSED: Attacker origin rejected (no CORS header)\")\n            return True\n        elif cors_origin == \"http://attacker.com\":\n            print(\"\\n❌ FAILED: Wildcard still allows any origin!\")\n            return False\n        else:\n            print(f\"\\n✓ Response origin: {cors_origin}\")\n            print(\"✓ PASSED: Only allowed origins accepted\")\n            return True\n        \n    except Exception as e:\n        print(f\"❌ ERROR: {str(e)}\")\n        return False\n\ndef test_localstorage_not_used():\n    \"\"\"Check if response doesn't contain tokens (they're in cookies)\"\"\"
    print_section(\"🔍 Test 4: Tokens Not in Response Body\")\n    \n    login_data = {\n        \"username\": \"admin\",\n        \"password\": \"test123\"\n    }\n    \n    try:\n        response = requests.post(\n            urljoin(API_BASE_URL, \"/login\"),\n            json=login_data,\n            headers={\"Origin\": FRONTEND_URL}\n        )\n        \n        try:\n            data = response.json()\n            \n            # Check if access_token is empty or not present\n            access_token = data.get(\"access_token\", \"\")\n            refresh_token = data.get(\"refresh_token\", \"\")\n            \n            if not access_token or access_token == \"\":\n                print(\"\\n✓ PASSED: access_token not returned in body (it's in cookie)\")\n            else:\n                print(f\"\\n⚠️  WARNING: access_token returned in body: {access_token[:50]}...\")\n                print(\"  Consider avoiding token in response body\")\n            \n            if not refresh_token or refresh_token == \"\":\n                print(\"✓ PASSED: refresh_token not returned in body (it's in cookie)\")\n                return True\n            else:\n                print(f\"⚠️  WARNING: refresh_token returned in body\")\n                return False\n                \n        except json.JSONDecodeError:\n            print(f\"Response is not JSON: {response.text[:100]}\")\n            return None\n        \n    except Exception as e:\n        print(f\"❌ ERROR: {str(e)}\")\n        return False\n\ndef main():\n    \"\"\"Run all tests\"\"\"
    print(\"\\n\" + \"=\"*60)\n    print(\"  🔒 SECURITY FIXES VERIFICATION SCRIPT\")\n    print(\"=\"*60)\n    print(f\"\\nAPI Base URL: {API_BASE_URL}\")\n    print(f\"Frontend URL: {FRONTEND_URL}\")\n    print(\"\\n⚠️  Make sure both frontend and backend are running!\")\n    \n    results = {}\n    \n    # Run tests\n    results[\"CORS Headers\"] = test_cors_headers()\n    results[\"HTTPOnly Cookies\"] = test_httponly_cookies()\n    results[\"Wildcard Rejection\"] = test_cors_wildcard_rejection()\n    results[\"Tokens Not in Body\"] = test_localstorage_not_used()\n    \n    # Summary\n    print_section(\"📊 TEST SUMMARY\")\n    \n    passed = sum(1 for v in results.values() if v is True)\n    failed = sum(1 for v in results.values() if v is False)\n    skipped = sum(1 for v in results.values() if v is None)\n    \n    for test_name, result in results.items():\n        status = \"✅ PASS\" if result is True else (\"❌ FAIL\" if result is False else \"⏭️  SKIP\")\n        print(f\"{status}: {test_name}\")\n    \n    print(f\"\\nTotal: {passed} passed, {failed} failed, {skipped} skipped\")\n    \n    if failed == 0:\n        print(\"\\n🎉 All critical security fixes verified!\")\n    else:\n        print(f\"\\n⚠️  {failed} test(s) failed. Review the output above.\")\n    \n    print(\"\\n\" + \"=\"*60)\n\nif __name__ == \"__main__\":\n    main()\n